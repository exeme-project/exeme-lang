{
  "customizations": {
    "codespaces": {
      "repositories": {
        "exeme-project/utils": {
          "permissions": "write-all"
        }
      }
    }
  },
  "onCreateCommand": "sudo apt update && sudo apt-get -y install clang-format ninja-build fonts-firacode && pip install -r ./requirements.txt && pre-commit install"
}
